/* Setup */
window.editorTopologyAssemble = topologyAssemble;
/* Setup END */

/* UI */
/* UI END */

function topologyAssemble(cells) {
    console.log(`Assembling topology: ${Object.assign(cells, Map)}`);

    return null;
}